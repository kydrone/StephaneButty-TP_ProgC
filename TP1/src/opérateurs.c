#include <stdio.h>
#include <stdbool.h>

int main() {
    int a = 16;
    int b = 3;
    int addition = a + b;
    int soustraction = a - b;
    int multiplication = a * b;
    int division = a / b;
    int modulo = a % b;
    printf("Valeurs initiales : ", a, b);
    printf("Addition (a + b) : ", addition);
    printf("Soustraction (a - b) :", soustraction);
    printf("Multiplication (a * b) : ", multiplication);
    printf("Division (a / b) : ", division);
    printf("Modulo (a %% b) : ", modulo);
    printf("a est-il égal à b ? : ", egal ? "true" : "false");
    printf("a est-il supérieur à b ? : ", superieur ? "true" : "false");
    return 0;
}
